import 'package:flutter/material.dart';

class SeasonDash extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Expanded(
      child: Center(
        child: Container(
          margin: EdgeInsets.symmetric(horizontal: 10),
          color: Colors.black,
          height: 5,
          width: 30,
        ),
      ),
    );
  }
}
